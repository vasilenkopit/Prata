// input.c -- ситуации, когда должен использоватьс€ символ &
#include <stdio.h>
int main(void)
{
    int age;             // переменна€
    float assets;        // переменна€
    char pet[30];        // строка

    printf("¬ведите информацию о своем возрасте, сумме в банке и любимом животном.\n");
    scanf("%d %f", &age, &assets);  // здесь должен быть указан символ &
    scanf("%s", pet);               // дл€ строкового массива символ & не нужен
    printf("%d $%.2f %s\n", age, assets, pet);

    return 0;
}
