/* animals.c -- использование оператора switch */
#include <stdio.h>
#include <ctype.h>
int main(void)
{
    char ch;

    printf("ƒайте мне букву алфавита, и € укажу вам ");
    printf("название животного,\nначинающеес€ с этой буквы.\n");
    printf("¬ведите букву или # дл€ завершени€.\n");
    while ((ch = getchar()) != '#')
    {
        if('\n' == ch)
            continue;
        if (islower(ch)) /* только строчные буквы */
            switch (ch)
            {
            case 'а' :
                printf("архар, дикий горный азиатский баран\n");
                break;
            case 'б' :
                printf("бабирусса, дика€ малайска€ свинь€\n");
                break;
            case 'к' :
                printf("коати, носуха обыкновенна€\n");
                break;
            case 'в' :
                printf("выхухоль, водоплавающее существо\n");
                break;
            case 'е' :
                printf("ехидна, игольчатый муравьед\n");
                break;
            case 'р' :
                printf("рыболов, светло-коричнева€ куница\n");
                break;
                default :
                printf("¬опрос озадачил!\n");
            }           /* конец оператора выбора */
        else
            printf("–аспознаютс€ только строчные буквы.\n");
        while (getchar() != '\n')
            continue;   /* пропустить оставшуюс€ часть входной строки */
        printf("¬ведите следующую букву или # дл€ завершени€.\n");
    }                   /* конец цикла while */
    printf("ƒо свидани€.\n");

    return 0;
}
