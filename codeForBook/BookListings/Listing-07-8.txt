/* paint.c -- использование условной операции */
#include <stdio.h>
#define COVERAGE 350        // число квадратных футов на одну банку краски
int main(void)
{
    int sq_feet;
    int cans;

    printf("¬ведите количество квадратных футов, которые необходимо покрасить:\n");
    while (scanf("%d", &sq_feet) == 1)
    {
        cans = sq_feet / COVERAGE;
        cans += ((sq_feet % COVERAGE == 0)) ? 0 : 1;
        printf("ƒл€ этого потребуетс€ %d %s краски.\n", cans,
                cans == 1 ? "банка" : "банки");
        printf("¬ведите следующее значение (или q дл€ завершени€):\n");
    }

    return 0;
}
